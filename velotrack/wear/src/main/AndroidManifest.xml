<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- This is what tells Google Play to send this artifact to watches and not to phones. -->
    <uses-feature
        android:name="android.hardware.type.watch"
        android:required="true" />

    <!--
        The watch records on its own when the phone is not there, so it needs the same location and
        foreground-service permissions the phone app has. GPS is NOT required: a watch without a
        GNSS chip still works as a display for a ride the phone is recording, which is the whole
        point of keeping both modes.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.VeloTrackWear">

        <!-- Declares this as a watch app that works without a phone-side install step. -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />

        <activity
            android:name=".WearMainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            The shared engine's own service, declared here so the watch can record without the
            phone. Same class as the phone runs; the manifest entry is per-app because a library
            module must not force a service into every consumer.
        -->
        <service
            android:name="com.fablab503.velotrack.recording.RecordingService"
            android:exported="false"
            android:foregroundServiceType="location" />
    </application>

</manifest>
