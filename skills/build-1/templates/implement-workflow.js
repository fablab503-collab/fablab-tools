export const meta = {
  name: 'velotrack-implement',
  description: 'Write the VeloTrack Android app modules in parallel against the plan contracts, then review the whole tree for compile and contract errors and apply fixes',
  phases: [
    { title: 'Implement', detail: 'eight module agents, each owning its own files' },
    { title: 'Review', detail: 'five read-only reviewers over the whole tree' },
    { title: 'Fix', detail: 'one fixer per module applies verified findings' },
  ],
}

const ROOT = '/Users/danielmadac/Documents/GitHub/fablab-tools'
const APP = `${ROOT}/velotrack`
const SRC = `${APP}/app/src/main/java/com/fablab503/velotrack`
const TEST = `${APP}/app/src/test/java/com/fablab503/velotrack`
const PLAN = `${ROOT}/docs/superpowers/plans/2026-09-07-velotrack-plan.md`
const SPEC = `${ROOT}/docs/superpowers/specs/2026-09-07-velotrack-design.md`
const RESEARCH = '/tmp/claude-501/research'

const COMMON = `You are implementing one module of VeloTrack, an offline GPS-only cycling map and tracker for Android (Kotlin, MapLibre Native 13.6.0 OpenGL, AGP 9.4.0 with built-in Kotlin 2.2.10, minSdk 26, compileSdk 36). There is NO compiler on this machine: the code is compiled later on GitHub Actions, so every import, signature and API name must be right the first time. Work slowly and precisely.

MANDATORY READING before writing anything:
1. The plan with binding module contracts: ${PLAN}
2. The design spec: ${SPEC}
3. The already-written shared files you must use and must NOT modify:
   ${SRC}/model/Models.kt, ${SRC}/settings/Prefs.kt, ${SRC}/recording/RideSession.kt
4. Build files (read-only for you): ${APP}/app/build.gradle.kts, ${APP}/gradle/libs.versions.toml
5. Research notes with verified API facts and snippets (read the ones relevant to your module): ${RESEARCH}/maplibre.md, ${RESEARCH}/android-toolchain.md, ${RESEARCH}/protomaps.md, ${RESEARCH}/gps-battery.md

HARD RULES:
- Implement every public signature in your module's section of the plan EXACTLY as written (names, parameter order, types, nullability, defaults). Other modules are being written in parallel against those signatures.
- Write ONLY the files you own (listed below). Never edit files owned by other modules or the shared files.
- Package root is com.fablab503.velotrack. Use the sub-package named in the plan.
- Only the dependencies in libs.versions.toml exist. No Room, kapt, KSP, Compose, Hilt, Gson, Moshi, Timber.
- Pure modules must not import android.* (JUnit tests run on the JVM). Use java.util.Locale.ROOT for number formatting to files.
- Handle nullable Android APIs; no '!!' on system services or intents.
- If you are unsure about an Android or MapLibre API name or signature, load WebFetch via ToolSearch and check the official docs (developer.android.com, maplibre.org/maplibre-native/android/api, raw GitHub source of maplibre-native). Do not guess.
- When done, re-read every file you wrote top to bottom looking for: missing imports, unbalanced braces, wrong types, unused/undefined names, Kotlin syntax errors, calls to functions that do not exist in the contracts.
- Return ONLY the structured result: the files you wrote (absolute paths), any deviation from the contract (should be empty; if a contract is impossible, explain), notes for reviewers, and cross-module resource names you referenced (string/drawable ids, intent extras).`

const RESULT_SCHEMA = {
  type: 'object',
  properties: {
    files: { type: 'array', items: { type: 'string' } },
    deviations: { type: 'array', items: { type: 'string' } },
    notes: { type: 'array', items: { type: 'string' } },
    cross_module_refs: { type: 'array', items: { type: 'string' } },
  },
  required: ['files', 'deviations', 'notes', 'cross_module_refs'],
}

const MODULES = [
  {
    key: 'A-geo', effort: 'medium',
    owns: `${SRC}/geo/*.kt and ${TEST}/geo/*.kt`,
    prompt: `Module A — geo (pure Kotlin). Files: ${SRC}/geo/Geo.kt, Simplify.kt, ElevationAccumulator.kt, ZoomRule.kt (ZoomRule object AND ZoomController class), plus tests ${TEST}/geo/GeoTest.kt, SimplifyTest.kt, ElevationAccumulatorTest.kt, ZoomRuleTest.kt (JUnit 4, org.junit.Assert). Use kotlin.math. RDP must be iterative (explicit stack), never recursive (tracks have 150k points). pointToSegmentM/projectOnSegment use a local equirectangular projection around point a (x = dLon*cos(latA)*111320, y = dLat*110540). Tests: Paris (48.8566,2.3522) to London (51.5074,-0.1278) ≈ 343.5 km within 1 km; bearing due east ≈ 90 (use two points on the equator); RDP of collinear points -> 2 points; radial keeps first and last; elevation ±3 m noise -> 0 gain, monotonic 0..20 m climb in 1 m steps -> gain ≈ 20 (within tolerance: with alpha smoothing the last anchor may lag, assert between 14 and 20); ZoomRule.targetZoom(0)=17.5, (20)=17.0, (100)=15.5, (25)=16.75; ZoomController: first call returns the target, an immediate second call with |delta|<hysteresis returns null, a gesture blocks for 10 s, rate limit caps change to maxRatePerSecond*elapsedSeconds.`,
  },
  {
    key: 'B-gpx', effort: 'medium',
    owns: `${SRC}/gpx/*.kt and ${TEST}/gpx/*.kt`,
    prompt: `Module B — gpx (pure Kotlin/JVM). Files: ${SRC}/gpx/GpxWriter.kt, ${SRC}/gpx/GpxParser.kt, tests ${TEST}/gpx/GpxWriterTest.kt, GpxParserTest.kt. Writer: stream through java.io.BufferedWriter(OutputStreamWriter(out, UTF-8)) and flush at the end (do not close out). Use java.time.Instant + DateTimeFormatter.ISO_INSTANT for isoTime. Use java.text.DecimalFormat with DecimalFormatSymbols(Locale.ROOT) patterns "0.000000" and "0.0". Escape &, <, >, ", ' in text. Parser: javax.xml.parsers.SAXParserFactory (set namespaceAware true; match on localName OR qName suffix so both namespaced and un-namespaced GPX work); collect rtept and trkpt lat/lon attributes and optional <ele> text; the track/route <name> (first one found) becomes GpxData.name; throw IllegalArgumentException on SAXException or when zero points. Disable external entities (setFeature XMLConstants.FEATURE_SECURE_PROCESSING true; wrap unsupported-feature exceptions). Tests: writer output contains header, metadata time, trk name, two trkseg when segment changes, ele before time, 6-decimal lat; parser reads a namespaced GPX 1.1 route with 3 rtept, a track with 2 trkseg (4 points total), rejects "not xml"; round-trip write->parse preserves point count and coordinates to 1e-6.`,
  },
  {
    key: 'C-route', effort: 'medium',
    owns: `${SRC}/route/*.kt and ${TEST}/route/*.kt`,
    prompt: `Module C — route (pure Kotlin). Files: ${SRC}/route/RouteFollower.kt, test ${TEST}/route/RouteFollowerTest.kt. Precompute cumulative distances per vertex with Geo.distanceM. update(): search segments in [lastSegment - windowSegments, lastSegment + windowSegments] (clamped); if the best distance in the window exceeds offRouteThresholdM, rescan all segments; distanceToRouteM = best perpendicular distance via Geo.pointToSegmentM; distanceRemainingM = totalDistanceM - (cumulative[seg] + t * segmentLength) using Geo.projectOnSegment; offRoute timing: track the timestamp when distance first exceeded the threshold; offRoute = true only when nowMs - firstExceededMs >= offRouteDelayMs; reset the timestamp when back within threshold. Handle routes with < 2 points (totalDistanceM 0, update returns distance to the single point, remaining 0). Tests: a straight 1 km north-south route: point at 30% along -> remaining ≈ 700 m (±5 m), distanceToRoute ≈ 0; a point 80 m east -> offRoute false at t=0, true after 10 s, false again immediately when back; a zig-zag route where the rider skips ahead beyond the window triggers the rescan and still finds the right segment.`,
  },
  {
    key: 'D-storage', effort: 'high',
    owns: `${SRC}/storage/*.kt`,
    prompt: `Module D — storage (Android). Files: ${SRC}/storage/TrackDatabase.kt, TrackRepository.kt, MapFileStore.kt, RouteStore.kt. SQLiteOpenHelper with the exact schema from the plan; enable WAL in onConfigure(db) via db.enableWriteAheadLogging(); use db.beginTransaction()/setTransactionSuccessful()/endTransaction() around batch inserts with SQLiteStatement for speed. TrackRepository.appendPoints continues seq from SELECT COALESCE(MAX(seq),-1)+1. pointsAsSequence loads chunks of 5000 rows with WHERE seq >= ? ORDER BY seq LIMIT 5000 (use kotlin sequence {} builder). listTracks computes pointCount with a subquery. MapFileStore: mapsDir = context.getExternalFilesDir("maps") ?: File(context.filesDir, "maps"), mkdirs; import is a suspend fun that runs on Dispatchers.IO: resolve DISPLAY_NAME and SIZE via ContentResolver.query with OpenableColumns, sanitize the name (keep [A-Za-z0-9._-], collapse others to '_', keep the original extension if .mbtiles/.pmtiles else throw IllegalArgumentException), copy via openInputStream to a temp file in mapsDir with a 1 MiB buffer calling onProgress(copied, total) at most every 4 MiB, then rename atomically; on failure delete the temp file. RouteStore: routesDir = File(context.filesDir, "routes"); importGpx(uri) suspend on IO: open stream, GpxParser.parse (module B contract: object GpxParser { fun parse(input: InputStream): GpxData } where GpxData(name: String?, points: List<LatLon>, elevations: List<Double?>)), compute distance with Geo.distanceM (module A contract), write points as little-endian doubles (lat, lon pairs) using java.nio.ByteBuffer via FileOutputStream.channel, insert the routes row, return RouteSummary; loadPoints reads the file back; delete removes row and file; activeRoute()/setActive use prefs.activeRouteId. Use ContentValues or SQLiteStatement; all cursors closed with use {}.`,
  },
  {
    key: 'E-recording', effort: 'high',
    owns: `${SRC}/location/*.kt, ${SRC}/recording/*.kt EXCEPT RideSession.kt, and ${TEST}/recording/*.kt`,
    prompt: `Module E — location + recording (Android; PointFilter and RideStats pure and tested). Files: ${SRC}/location/LocationExt.kt, GpsSource.kt, HeadingEstimator.kt; ${SRC}/recording/PointFilter.kt, RideStats.kt, RecordingService.kt, RideController.kt; tests ${TEST}/recording/PointFilterTest.kt, RideStatsTest.kt.
GpsSource: androidx.core.location.LocationManagerCompat.requestLocationUpdates(locationManager, LocationManager.GPS_PROVIDER, LocationRequestCompat.Builder(1000L).setQuality(LocationRequestCompat.QUALITY_HIGH_ACCURACY).setMinUpdateIntervalMillis(1000L).setMinUpdateDistanceMeters(0f).setMaxUpdateDelayMillis(0L).build(), ContextCompat.getMainExecutor(context), listener) where listener implements androidx.core.location.LocationListenerCompat (override onLocationChanged(Location), onProviderEnabled/Disabled to update GpsStatus.providerEnabled); GnssStatus.Callback registered with registerGnssStatusCallback(executor, cb) on API 30+ else registerGnssStatusCallback(cb, Handler(Looper.getMainLooper())) (API 24+ overload); count usedInFix(i); annotate with @SuppressLint("MissingPermission") after checking hasPermission(). hasPermission() = ContextCompat.checkSelfPermission(ACCESS_FINE_LOCATION) == GRANTED. isGpsEnabled = LocationManagerCompat.isLocationEnabled(lm) && lm.isProviderEnabled(GPS_PROVIDER).
LocationExt.toGpsFix(): ele = if (Build.VERSION.SDK_INT >= 34 && hasMslAltitude()) mslAltitudeMeters else if (hasAltitude()) altitude else null; verticalAccuracyM if hasVerticalAccuracy(); speed if hasSpeed(); bearing if hasBearing(); bearingAccuracy if hasBearingAccuracy(); accuracy if hasAccuracy(); elapsedNs = elapsedRealtimeNanos; timeMs = time.
HeadingEstimator: per contract; heading updates only when speed > minSpeedMps, bearingDeg != null, and (bearingAccuracyDeg == null || < maxBearingAccuracyDeg).
PointFilter and RideStats: exactly the rules in the plan; keep them free of android imports; use Geo.haversineM from module A (contract: object Geo { fun haversineM(lat1: Double, lon1: Double, lat2: Double, lon2: Double): Double }) and ElevationAccumulator from module A (contract: class ElevationAccumulator(thresholdM: Double = 5.0, alpha: Double = 0.3, maxVerticalAccuracyM: Float = 20f) { val gainM: Double; val lossM: Double; fun add(ele: Double, verticalAccuracyM: Float?); fun reset() }). RideStats.addStored skips the distance increment when the new point's segment differs from the previous point's segment. RideStats.rebuild replays points.
RecordingService: extends androidx.lifecycle.LifecycleService; call super.onStartCommand(intent, flags, startId) first (LifecycleService requires it). Follow the plan precisely for actions, foreground start (androidx.core.app.ServiceCompat.startForeground with android.content.pm.ServiceInfo.FOREGROUND_SERVICE_TYPE_LOCATION when Build.VERSION.SDK_INT >= 30 else 0, inside try/catch of Exception: on failure stopSelf and return START_NOT_STICKY), PARTIAL_WAKE_LOCK "VeloTrack::Recording" acquire(12*60*60*1000L), NotificationChannel "velotrack.recording" IMPORTANCE_LOW, NotificationCompat.Builder with setSmallIcon(R.drawable.ic_stat_rec), setOngoing(true), setOnlyAlertOnce(true), setContentIntent to com.fablab503.velotrack.ui.MainActivity (PendingIntent.FLAG_IMMUTABLE), actions built from PendingIntent.getService with FLAG_IMMUTABLE. String resources you may use (module G creates them): R.string.app_name, R.string.notif_channel_recording, R.string.notif_recording_title, R.string.notif_paused_title, R.string.notif_action_pause, R.string.notif_action_resume, R.string.notif_action_stop, R.string.notif_storage_error. Format distance in the notification yourself as "%.1f km" with Locale.ROOT via String.format. Use TrackRepository/TrackDatabase from module D (contract in plan), Prefs for FilterConfig (accuracyCutoffM, autoPauseEnabled), RideSession for state. Do database work on a single-thread dispatcher (Executors.newSingleThreadExecutor().asCoroutineDispatcher()) from lifecycleScope; GPS callbacks arrive on the main thread. Moving time: add elapsed between consecutive accepted fixes while status == RECORDING (not while auto-paused or paused). Track name: "Ride yyyy-MM-dd HH:mm" with java.text.SimpleDateFormat(Locale.ROOT). Set RideSession.serviceRunning true in onCreate, false in onDestroy; RideSession.update on every fix and status change; on STOP set status IDLE but keep stats visible until the next start. RideController delegates to the service companion functions.
Tests: PointFilter — rejects accuracy 80 m, accepts 10 m; first accepted fix stored; a fix 2 m away not stored, 6 m away stored; a 500 m jump in 1 s rejected; speed 0.5 m/s for 11 s -> autoPaused true, then 2 m/s -> false; gap 250 m -> newSegment and segment incremented. RideStats — two stored points 1 km apart -> distance ≈ 1000 (±2); segment change skips distance; max speed tracked; snapshot avg = distance/moving time.`,
  },
  {
    key: 'F-map', effort: 'high',
    owns: `${SRC}/map/*.kt and ${TEST}/map/*.kt`,
    prompt: `Module F — map (Android; MapUrl and StyleTemplate pure and tested). Files: ${SRC}/map/MapUrl.kt, StyleTemplate.kt, MapController.kt; tests ${TEST}/map/MapUrlTest.kt, StyleTemplateTest.kt.
MapUrl.forFile: ".mbtiles" -> "mbtiles://" + encodedPath (path already starts with "/", giving mbtiles:///abs/...); ".pmtiles" -> "pmtiles://file://" + encodedPath; percent-encode each path segment for characters outside [A-Za-z0-9._~/-] using UTF-8 bytes (write a small encoder; do not use java.net.URLEncoder which turns spaces into '+'). Tests: "/storage/emulated/0/Android/data/x/files/maps/berlin.mbtiles" -> "mbtiles:///storage/emulated/0/Android/data/x/files/maps/berlin.mbtiles"; a space becomes %20; ".pmtiles" -> "pmtiles://file:///..."; ".txt" throws IllegalArgumentException.
StyleTemplate.render: string-based; when mapUrl != null replace every "{MAP_URL}" with it; when null replace the exact substring "\\"url\\": \\"{MAP_URL}\\"" (and also the compact variant "\\"url\\":\\"{MAP_URL}\\"") with "\\"tiles\\": []". Tests cover both.
MapController: read the research file ${RESEARCH}/maplibre.md carefully (verified 13.6.0 API: packages org.maplibre.android.*; MapLibreMap, MapView, Style, CameraPosition.Builder(target/zoom/tilt/bearing/padding), CameraUpdateFactory.newCameraPosition, easeCamera(update, durationMs, easingInterpolator=false), GeoJsonSource(id, FeatureCollection, GeoJsonOptions), setGeoJson(FeatureCollection|Feature), LineLayer/SymbolLayer with PropertyFactory, style.addImage(String, Bitmap), Property.ICON_ROTATION_ALIGNMENT_MAP, org.maplibre.geojson.Feature/FeatureCollection/LineString/Point, mapView.setMaximumFps(30), map.setPrefetchesTiles(false), map.uiSettings, map.addOnCameraMoveStartedListener { reason -> if (reason == MapLibreMap.OnCameraMoveStartedListener.REASON_API_GESTURE) ... }, mapView.addOnDidFailLoadingMapListener { errorMessage -> ... }, MapLibreMap.OnMapClickListener). Verify any method you are not certain about by fetching https://maplibre.org/maplibre-native/android/api/ pages or the raw source under https://raw.githubusercontent.com/maplibre/maplibre-native/main/platform/android/MapLibreAndroid/src/main/java/org/maplibre/android/. Implement the contract: onMapReady stores the map, uiSettings: compass/logo/attribution disabled, tilt gestures enabled, rotate gestures enabled; setMaximumFps(30); setPrefetchesTiles(false); gesture listener sets cameraMode = FREE (remember the previous follow mode for recenter), notifies onUserGesture and ZoomController.onUserGesture. loadStyle: read context.assets.open("style.json"), render with the map URL, map.setStyle(Style.Builder().fromJson(json)) { style -> add sources "route","track-history","track-live","puck" (GeoJsonOptions().withBuffer(64).withTolerance(0.5f) for lines), layers "route-line" (orange #FF9800 width 6, below track), "track-history-line" (#42A5F5 width 5), "track-live-line" (#42A5F5 width 5), "puck-layer" (SymbolLayer with iconImage("puck"), iconRotationAlignment MAP, iconAllowOverlap, iconIgnorePlacement, iconSize 1f); addImage("puck", drawPuckBitmap()); re-apply any pending route/track/puck data; onDone(null) }, and register a fail-loading listener once in onMapReady that forwards the message to the pending onDone(message). drawPuckBitmap(): 96x96 px android.graphics.Bitmap ARGB_8888, Canvas, Path arrow pointing up, fill #FFFFFF with a #000000 stroke, plus a semi-transparent blue circle behind. updatePosition(fix, headingDeg, nowMs): puck feature setGeoJson(Feature point) and (puck layer).setProperties(iconRotate(heading ?: lastHeading ?: 0f)); camera per mode: FOLLOW_3D -> zoom from ZoomController.next(speedKmh, currentZoom, nowMs) ?: currentZoom (initial 17.0), tilt prefs.pitchDeg, bearing heading (hold last), padding top = mapView.height*0.55 set once via CameraPosition.Builder.padding(0.0, top, 0.0, 0.0) (re-set when height changes), easeCamera(newCameraPosition, 1000, false); FOLLOW_2D -> zoom 15.5, tilt 0, bearing 0, padding zeros; FREE -> nothing. recenter(): cameraMode = previous follow mode (default prefs.followMode) and immediately move camera. setTrackHistory/appendTrackPoint/clearTrack/setRoute per plan (history simplified with Simplify.rdp(points, 2.0) from module A; live list capped at 500 then merged into history; data kept in fields so a style reload re-applies it). onDestroy clears references. All MapLibre calls only after onMapReady and style loaded (guard with null checks and a 'styleReady' flag).`,
  },
  {
    key: 'G-ui', effort: 'high',
    owns: `${SRC}/ui/*.kt, ${SRC}/VeloTrackApp.kt, ${APP}/app/src/main/AndroidManifest.xml, ${APP}/app/src/main/res/**`,
    prompt: `Module G — ui (Android). You own: ${SRC}/VeloTrackApp.kt; ${SRC}/ui/MainActivity.kt, TracksActivity.kt, MapFilesActivity.kt, SettingsActivity.kt (+ SettingsFragment in the same file or its own), AboutDialog.kt, Format.kt; ${APP}/app/src/main/AndroidManifest.xml; ${APP}/app/src/main/res/** (layout/, values/strings.xml, values/themes.xml, values/colors.xml, drawable/, mipmap-anydpi-v26/ic_launcher.xml + ic_launcher_round.xml, xml/file_paths.xml, xml/preferences.xml, menu/main_menu.xml).
Follow the plan's Module G section and manifest spec exactly. Other modules' contracts you call are in the plan (RecordingService companion start/pause/resume/stop or RideController; RideSession.state; GpsSource(context).start(listener)/stop(); HeadingEstimator; MapController(context, mapView, prefs) with onMapReady/loadStyle/updatePosition/recenter/setTrackHistory/appendTrackPoint/clearTrack/setRoute/cameraMode/onUserGesture/onDestroy; TrackRepository; MapFileStore; RouteStore; RouteFollower(points, offRouteThresholdM, offRouteDelayMs).update(pos, nowMs); GpxWriter.write(out, name, sequence); Format). Read ${RESEARCH}/maplibre.md for MapView lifecycle (onCreate/onStart/onResume/onPause/onStop/onSaveInstanceState/onLowMemory/onDestroy must all be forwarded) and MapLibreMapOptions; read ${RESEARCH}/android-toolchain.md for permissions, FileProvider, SAF, and the foreground-service start order (request permissions, then start the service from the visible activity).
Strings module E relies on (must exist with these exact names): app_name ("VeloTrack"), notif_channel_recording, notif_recording_title, notif_paused_title, notif_action_pause, notif_action_resume, notif_action_stop, notif_storage_error. Drawable module E relies on: ic_stat_rec (vector, 24dp, white filled circle). Intent extra constant in MainActivity companion: EXTRA_VIEW_TRACK_ID = "view_track_id".
preferences.xml must match Prefs exactly: key "accuracy_cutoff_m" ListPreference with string values 20/30/50/100 (default "50"); "auto_pause" SwitchPreferenceCompat default true; "screen_mode" ListPreference values KEEP_ON/DIM/SYSTEM; "dim_level" SeekBarPreference 5..100 default 20; "units" ListPreference METRIC/IMPERIAL; "pitch_deg" SeekBarPreference 0..60 default 55; "off_route_m" ListPreference values 30/50/100/200 (default "50"). Use android:defaultValue matching the Prefs defaults.
Theme: parent Theme.Material3.Dark.NoActionBar, android:windowBackground @android:color/black, colorPrimary #42A5F5, statusBarColor black. Main layout: FrameLayout id mapContainer (MapView is created programmatically in onCreate with MapLibreMapOptions.createFromAttributes(this).attributionEnabled(false).logoEnabled(false).compassEnabled(false).setPrefetchesTiles(false) and added at index 0; MapLibre.getInstance is called in VeloTrackApp.onCreate followed by MapLibre.setConnected(false)), a top HUD panel (semi-transparent black) with TextViews for speed (72sp), unit, distance, moving time, avg speed, elevation gain, clock, gps, battery; bottom controls: MaterialButtons/FABs for record/pause/stop, recenter, 2D/3D toggle, and an overflow menu button (PopupMenu from res/menu/main_menu.xml: Tracks, Import route (GPX), Clear route, Map files, Settings, About). Battery via BatteryManager.BATTERY_PROPERTY_CAPACITY; clock updated once per minute with a Handler. Collect RideSession.state with lifecycleScope.launch { repeatOnLifecycle(Lifecycle.State.STARTED) { RideSession.state.collect { render(it) } } }. When RideSession.state.status == IDLE and the service is not running, drive the map from a local GpsSource + HeadingEstimator; when recording, use state.lastFix/headingDeg and stop the local GpsSource (avoid two GPS clients). On start: check ACCESS_FINE_LOCATION (request fine+coarse and POST_NOTIFICATIONS on 33+ via ActivityResultContracts.RequestMultiplePermissions), then RideController.startNew(this). Battery Saver warning: PowerManager.isPowerSaveMode && (SDK>=28 && locationPowerSaveMode in {LOCATION_MODE_GPS_DISABLED_WHEN_SCREEN_OFF, LOCATION_MODE_ALL_DISABLED_WHEN_SCREEN_OFF}) -> AlertDialog once per session. Screen mode from Prefs applied in onResume (FLAG_KEEP_SCREEN_ON; window.attributes.screenBrightness for DIM). Recovery dialog: if TrackRepository(TrackDatabase(this)).findUnfinished() != null && !RideSession.serviceRunning -> Resume / Finish. GPS-off banner via GpsSource.isGpsEnabled with a button to Settings.ACTION_LOCATION_SOURCE_SETTINGS. No-map overlay with a button to MapFilesActivity when MapFileStore.activeMap() == null. Viewing a track (EXTRA_VIEW_TRACK_ID): load points on IO, setTrackHistory, and move the camera to the first point in FOLLOW_2D/FREE. Route following: when RouteStore.activeRoute() != null, load points, MapController.setRoute, create RouteFollower(points, prefs.offRouteM, 10_000), update per fix, show remaining distance in the HUD, vibrate on off-route transitions (VibratorManager on 31+, else Vibrator; VibrationEffect.createWaveform). TracksActivity: ListView + ArrayAdapter over TrackSummary strings using Format; tap -> MainActivity with EXTRA_VIEW_TRACK_ID; long press -> AlertDialog with Rename/Export share/Export save-as/Delete; export writes GPX with GpxWriter to File(getExternalFilesDir("tracks"), "<safe-name>.gpx") on IO then FileProvider.getUriForFile(this, "$packageName.fileprovider", file) + ACTION_SEND type application/gpx+xml, or CreateDocument("application/gpx+xml") copying the file. MapFilesActivity: list files with sizes, tap -> set active (and MainActivity reloads style on resume), long press -> delete, button Import -> OpenDocument(arrayOf("*/*")) then MapFileStore.import with a ProgressDialog-like AlertDialog showing MB copied. AboutDialog: versionName from packageManager.getPackageInfo, text: "Map data © OpenStreetMap contributors (ODbL) — https://www.openstreetmap.org/copyright. Style: Protomaps (BSD-3). Renderer: MapLibre Native (BSD-2). Fonts: Noto Sans (OFL 1.1)." Manifest also declares android:allowBackup="false", android:supportsRtl="true", android:icon/@mipmap/ic_launcher and roundIcon. Launcher icon: adaptive icon with <background android:drawable="@color/black_bg"/> and a vector foreground drawable/ic_launcher_foreground.xml (simple white arrow/chevron). Every string user-visible in strings.xml. Use ViewBinding (ActivityMainBinding etc.) for layouts you create; layout file names activity_main.xml, activity_tracks.xml, activity_map_files.xml, activity_settings.xml.`,
  },
  {
    key: 'H-tools-ci', effort: 'high',
    owns: `${APP}/tools/*, ${ROOT}/.github/workflows/*, ${APP}/README.md, ${APP}/THIRD_PARTY.md`,
    prompt: `Module H — tools, CI workflows, docs. Files: ${APP}/tools/package.json, ${APP}/tools/gen-style.mjs, ${APP}/tools/build-assets.sh (chmod +x), ${ROOT}/.github/workflows/velotrack-android.yml, ${ROOT}/.github/workflows/velotrack-map-extract.yml, ${APP}/README.md, ${APP}/THIRD_PARTY.md. Read ${RESEARCH}/protomaps.md and ${RESEARCH}/android-toolchain.md thoroughly: they contain verified action versions (actions/checkout@v7, actions/setup-java@v6 temurin 17, gradle/actions/setup-gradle@v6 with gradle-version 9.7.1, actions/upload-artifact@v4; use actions/setup-node@v5 with node-version 22 — verify v5 exists via WebFetch of https://api.github.com/repos/actions/setup-node/releases/latest, else use the latest major), the @protomaps/basemaps 5.7.2 API (import { layers, namedFlavor } from "@protomaps/basemaps"; layers("protomaps", namedFlavor("dark"), { lang })), the basemaps-assets URLs, the pmtiles CLI 1.31.2 tarball URL and extract syntax, the builds.json index at https://build-metadata.protomaps.dev/builds.json, and gh release semantics (gh release view || gh release create; gh release upload --clobber; permissions: contents: write; GH_TOKEN: github.token).
gen-style.mjs: as in the plan; source "protomaps" { type: "vector", url: "{MAP_URL}", attribution: "© OpenStreetMap contributors" }; glyphs "asset://fonts/{fontstack}/{range}.pbf"; sprite "asset://sprites/v4/dark"; patch background-color of the layer with type "background" to "#000000"; for layers with id "earth" set paint["fill-color"]="#000000"; drop layers whose id starts with "pois"; write to process.argv[2] (output path). package.json: {"name":"velotrack-tools","private":true,"type":"module","dependencies":{"@protomaps/basemaps":"5.7.2"}}. Verify the actual export names of @protomaps/basemaps 5.7.2 by fetching https://raw.githubusercontent.com/protomaps/basemaps/main/styles/src/index.ts (and confirm the package's "exports"/"main" in its package.json on unpkg or raw GitHub styles/package.json) — must be right because CI runs it.
build-assets.sh: set -euo pipefail; cd to its own dir (tools/); npm install --no-audit --no-fund; node gen-style.mjs ../app/src/main/assets/style.json (mkdir -p); download sprites v4 dark{,@2x}.{json,png} and fonts: for each of "Noto Sans Regular" "Noto Sans Medium" "Noto Sans Italic" all 256 ranges start=0..65280 step 256 as "<start>-<start+255>.pbf" from https://raw.githubusercontent.com/protomaps/basemaps-assets/main/fonts/<url-encoded stack>/<range>.pbf, plus fonts/OFL.txt; generate the URL list to a temp file and download with xargs -P 16 -n 2 curl -fsSL --retry 3 -o <dest> <url>, skipping existing files; verify each stack dir has 256 files; print du -sh. Note curl -o with paths containing spaces: quote via xargs -d '\\n' or use a helper function with bash -c.
velotrack-android.yml: name "velotrack-android"; on push branches [main] paths ["velotrack/**", ".github/workflows/velotrack-android.yml"], workflow_dispatch; permissions contents: write; job build on ubuntu-latest; steps: checkout; setup-java temurin 17; setup-gradle gradle-version 9.7.1; setup-node 22; run tools/build-assets.sh (working-directory velotrack); run gradle --no-daemon testDebugUnitTest assembleRelease (working-directory velotrack, env GITHUB_RUN_NUMBER is automatic); a step that fails if the merged manifest contains android.permission.INTERNET: grep -L or 'if grep -q "android.permission.INTERNET" velotrack/app/build/intermediates/merged_manifests/release/*/AndroidManifest.xml' — use find to locate AndroidManifest.xml under merged_manifests/release; also print the permissions it does contain; upload-artifact name velotrack-apk path velotrack/app/build/outputs/apk/release/*.apk; publish step (only on push to main): APK=$(ls velotrack/app/build/outputs/apk/release/*.apk | head -n1); cp to velotrack-v\${GITHUB_RUN_NUMBER}.apk and velotrack-latest.apk; create/update release tag velotrack-v\${GITHUB_RUN_NUMBER} (immutable, --target $GITHUB_SHA, notes with commit) and the rolling pre-release velotrack-latest: if it exists -> gh release upload velotrack-latest velotrack-latest.apk --clobber and gh release edit velotrack-latest --target? (gh release edit has no --target; instead delete and recreate the tag: git tag -f velotrack-latest && git push -f origin velotrack-latest, then gh release upload --clobber) — implement robustly: 'git tag -f velotrack-latest $GITHUB_SHA && git push -f origin velotrack-latest; if gh release view velotrack-latest; then gh release upload velotrack-latest velotrack-latest.apk --clobber; gh release edit velotrack-latest --notes "..."; else gh release create velotrack-latest velotrack-latest.apk --prerelease --title "VeloTrack latest" --notes "..."; fi'. Also upload the test report on failure (actions/upload-artifact with if: failure(), path velotrack/app/build/reports/tests).
velotrack-map-extract.yml: workflow_dispatch inputs name (required, pattern hint), bbox (required), maxzoom (default "15"), build (default "" = latest); permissions contents: write; steps: checkout (not strictly needed but harmless); install pmtiles 1.31.2 from the release tarball; resolve BUILD from builds.json with jq when empty; pmtiles extract --dry-run then real extract with --download-threads=8; pip install pmtiles==3.7.0 (actions/setup-python@v5 with python 3.12 — verify latest major via WebFetch); pmtiles-convert NAME.pmtiles NAME.mbtiles; pmtiles verify/show; check sizes < 2000000000 bytes else fail with a helpful message; gh release create/upload to tag map-<name> (create the tag at HEAD if needed: git tag -f map-<name> && git push -f origin map-<name>) with both files, --clobber; write a summary to $GITHUB_STEP_SUMMARY with sizes and download URLs.
README.md: what the app is; install (Releases page -> velotrack-latest -> download .apk on the phone -> allow installing from browser); get a map (bboxfinder.com to get minLon,minLat,maxLon,maxLat -> Actions -> velotrack-map-extract -> Run workflow -> download <name>.mbtiles from the map-<name> release on the phone -> in app: menu -> Map files -> Import); ride (airplane mode recommended; first fix may take minutes without internet; recording runs with screen off; battery saver note); settings summary; privacy (no INTERNET permission, all data local); building locally (needs Android Studio, run tools/build-assets.sh first); limitations (no turn-by-turn; labels language via LANG env in build-assets); licences pointer. THIRD_PARTY.md: MapLibre Native (BSD-2), Protomaps basemaps style (BSD-3), basemaps-assets sprites (MIT-derived), Noto Sans (OFL 1.1), OpenStreetMap data (ODbL, attribution text), AndroidX/Material (Apache-2.0), kotlinx-coroutines (Apache-2.0), pmtiles tools (BSD-3).`,
  },
]

phase('Implement')
const implResults = await parallel(MODULES.map(m => () =>
  agent(`${COMMON}\n\nFILES YOU OWN: ${m.owns}\n\n${m.prompt}`,
    { label: `impl:${m.key}`, phase: 'Implement', schema: RESULT_SCHEMA, effort: m.effort })
    .then(r => (r ? { key: m.key, ...r } : null))))

const implOk = implResults.filter(Boolean)
log(`Implement: ${implOk.length}/${MODULES.length} modules reported; deviations: ${implOk.flatMap(r => r.deviations).length}`)
const crossRefs = implOk.flatMap(r => r.cross_module_refs.map(x => `[${r.key}] ${x}`))
const deviations = implOk.flatMap(r => r.deviations.map(x => `[${r.key}] ${x}`))
const implNotes = implOk.flatMap(r => r.notes.map(x => `[${r.key}] ${x}`))

phase('Review')
const REVIEW_SCHEMA = {
  type: 'object',
  properties: {
    findings: {
      type: 'array',
      items: {
        type: 'object',
        properties: {
          file: { type: 'string' },
          line: { type: 'integer' },
          severity: { type: 'string', enum: ['compile', 'runtime', 'contract', 'minor'] },
          problem: { type: 'string' },
          fix: { type: 'string' },
        },
        required: ['file', 'line', 'severity', 'problem', 'fix'],
      },
    },
    summary: { type: 'string' },
  },
  required: ['findings', 'summary'],
}

const REVIEW_COMMON = `You are reviewing the freshly written VeloTrack Android app (Kotlin, AGP 9.4.0 built-in Kotlin 2.2.10, minSdk 26, compileSdk 36, MapLibre Native android-sdk-opengl 13.6.0). There is no compiler on this machine; the code will be compiled on GitHub Actions, so your job is to find what the compiler or the runtime would reject BEFORE that. Read-only: do NOT edit any file. Project: ${APP} (sources under ${SRC}, tests under ${TEST}, resources under ${APP}/app/src/main/res, manifest ${APP}/app/src/main/AndroidManifest.xml, build files ${APP}/app/build.gradle.kts and ${APP}/gradle/libs.versions.toml, tools ${APP}/tools, workflows ${ROOT}/.github/workflows). Contracts: ${PLAN}. Verified API facts: ${RESEARCH}/*.md. Use Grep/Read on every relevant file; when unsure about an API, load WebFetch via ToolSearch and verify against official docs or source. Report only real, specific problems with file, line, what breaks, and the exact fix. No style nits. Deviations reported by the module authors: ${JSON.stringify(deviations)}. Cross-module references the authors declared: ${JSON.stringify(crossRefs)}. Author notes: ${JSON.stringify(implNotes).slice(0, 6000)}`

const REVIEWS = [
  { key: 'kotlin-compile', prompt: `${REVIEW_COMMON}\n\nLENS: Kotlin compile correctness across EVERY .kt file under ${SRC} and ${TEST}: missing or wrong imports (exact package of every class, e.g. androidx.core.location.LocationManagerCompat, org.maplibre.android.maps.MapLibreMap, org.maplibre.geojson.Point), unresolved references, type mismatches (Float vs Double vs Int, nullable misuse), wrong named arguments, missing overrides (LifecycleService.onStartCommand must call super; LocationListenerCompat), unbalanced braces, duplicate declarations, when-expressions not exhaustive, suspend functions called outside coroutines, lateinit on primitives, val reassignment, smart-cast on mutable properties, and JUnit 4 test syntax (org.junit.Test, org.junit.Assert.*).` },
  { key: 'contracts', prompt: `${REVIEW_COMMON}\n\nLENS: Cross-module contract conformance. For every public signature listed in the plan, check the implementation matches EXACTLY (name, params, types, defaults) and that every call site in other modules uses the same names/types (e.g. ui -> MapController, RecordingService companion, TrackRepository, MapFileStore, RouteStore, RouteFollower, GpxWriter, Format; recording -> Geo, ElevationAccumulator, TrackRepository; storage -> GpxParser, Geo). Check R.string / R.drawable / R.layout / R.id / R.menu names used in Kotlin exist in res/, and that ViewBinding class names match layout file names (activity_main.xml -> ActivityMainBinding) and view ids. Check preferences.xml keys/types match Prefs. Check the manifest declares every Activity and the service with the right class names, and that RideSession/Models are used as defined.` },
  { key: 'android-runtime', prompt: `${REVIEW_COMMON}\n\nLENS: Android API and runtime correctness: foreground service start order and ServiceCompat.startForeground type usage, permissions in manifest vs runtime requests, notification channel before notify, PendingIntent FLAG_IMMUTABLE, LocationManagerCompat/LocationRequestCompat usage, GnssStatus callback registration per API level, MapView lifecycle forwarding (all 8 methods), MapLibre.getInstance before MapView creation, MapLibre 13.6.0 API names (verify against ${RESEARCH}/maplibre.md and the official API docs), SQLite cursor handling, coroutine dispatchers for IO, SAF/ContentResolver usage, FileProvider paths matching getExternalFilesDir("tracks"), Vibrator APIs per level, window brightness range, config-change handling. Also confirm the manifest strips INTERNET and ACCESS_WIFI_STATE with tools:node="remove" and declares xmlns:tools.` },
  { key: 'build-ci', prompt: `${REVIEW_COMMON}\n\nLENS: Build and CI correctness: ${APP}/app/build.gradle.kts and libs.versions.toml (AGP 9 DSL validity, no Kotlin plugin, R8 rules), ${APP}/tools/gen-style.mjs against the real @protomaps/basemaps 5.7.2 exports (fetch https://raw.githubusercontent.com/protomaps/basemaps/main/styles/src/index.ts and https://raw.githubusercontent.com/protomaps/basemaps/main/styles/package.json to confirm export names and that the package resolves as ESM), ${APP}/tools/build-assets.sh (bash correctness, quoting of paths with spaces, xargs usage, URL encoding of font stack names, exit codes), and both workflow YAML files (valid YAML, correct action versions that exist, correct working-directory, expressions, gh commands, permissions, artifact paths, merged-manifest path under app/build/intermediates/merged_manifests/release/). Also confirm assets/style.json's placeholder string equals StyleTemplate.PLACEHOLDER and that MapController reads "style.json" from assets.` },
  { key: 'logic-tests', prompt: `${REVIEW_COMMON}\n\nLENS: Logic and tests: PointFilter, RideStats, HeadingEstimator, RouteFollower, Geo, Simplify, ElevationAccumulator, ZoomController, GpxWriter/Parser, MapUrl, StyleTemplate — check each unit test asserts values that the implementation actually produces (compute by hand), check off-by-one and unit errors (m/s vs km/h, ms vs s, degrees vs radians), division by zero (moving time 0), first-fix edge cases, segment handling, and that tests do not depend on Android classes. Report tests that would fail or code that is wrong.` },
]

const reviews = (await parallel(REVIEWS.map(r => () =>
  agent(r.prompt, { label: `review:${r.key}`, phase: 'Review', schema: REVIEW_SCHEMA, effort: 'high' })
    .then(x => (x ? { key: r.key, ...x } : null))))).filter(Boolean)

const allFindings = reviews.flatMap(r => r.findings.map(f => ({ ...f, lens: r.key })))
log(`Review: ${allFindings.length} findings (${allFindings.filter(f => f.severity === 'compile').length} compile, ${allFindings.filter(f => f.severity === 'contract').length} contract, ${allFindings.filter(f => f.severity === 'runtime').length} runtime, ${allFindings.filter(f => f.severity === 'minor').length} minor)`)

phase('Fix')
const FIX_SCHEMA = {
  type: 'object',
  properties: {
    fixed: { type: 'array', items: { type: 'string' } },
    rejected: { type: 'array', items: { type: 'object', properties: { finding: { type: 'string' }, reason: { type: 'string' } }, required: ['finding', 'reason'] } },
  },
  required: ['fixed', 'rejected'],
}

function ownerOf(file) {
  if (file.includes('/geo/')) return 'A-geo'
  if (file.includes('/gpx/')) return 'B-gpx'
  if (file.includes('/route/')) return 'C-route'
  if (file.includes('/storage/')) return 'D-storage'
  if (file.includes('/location/') || file.includes('/recording/')) return 'E-recording'
  if (file.includes('/map/')) return 'F-map'
  if (file.includes('/ui/') || file.includes('VeloTrackApp') || file.includes('AndroidManifest') || file.includes('/res/')) return 'G-ui'
  if (file.includes('/tools/') || file.includes('/.github/') || file.endsWith('README.md') || file.endsWith('THIRD_PARTY.md')) return 'H-tools-ci'
  return 'X-build'
}

const groups = {}
for (const f of allFindings) {
  if (f.severity === 'minor') continue
  const k = ownerOf(f.file)
  ;(groups[k] ||= []).push(f)
}
const groupKeys = Object.keys(groups)
log(`Fix: ${groupKeys.length} module groups: ${groupKeys.map(k => `${k}(${groups[k].length})`).join(', ')}`)

const fixes = (await parallel(groupKeys.map(k => () =>
  agent(`You are fixing review findings in one module of the VeloTrack Android app at ${APP} (contracts: ${PLAN}; verified API facts: ${RESEARCH}/*.md). No compiler exists here; be precise. For EACH finding below: first open the file and verify the problem is real (reviewers can be wrong); if real, apply the smallest correct fix; if the fix would change a public contract signature, do NOT change the signature — fix the call site or implementation instead, and explain. Never touch shared files ${SRC}/model/Models.kt, ${SRC}/settings/Prefs.kt, ${SRC}/recording/RideSession.kt. Only edit files owned by module ${k} unless a finding explicitly concerns another file listed here. After editing, re-read each changed file fully for syntax and import correctness. Return the list of fixed findings and rejected ones with reasons.\n\nFINDINGS:\n${JSON.stringify(groups[k], null, 1)}`,
    { label: `fix:${k}`, phase: 'Fix', schema: FIX_SCHEMA, effort: 'high' })
    .then(x => (x ? { key: k, ...x } : null))))).filter(Boolean)

return {
  modules: implOk.map(r => ({ key: r.key, files: r.files.length, deviations: r.deviations, notes: r.notes })),
  crossRefs,
  reviewSummaries: reviews.map(r => ({ key: r.key, count: r.findings.length, summary: r.summary })),
  minorFindings: allFindings.filter(f => f.severity === 'minor'),
  fixes,
}